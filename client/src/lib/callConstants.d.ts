/* eslint-disable no-unused-vars */
export enum CALL_STATUS {
  INITIATED = "initiated",
  ANSWERED = "answered",
  DENIED = "denied",
  CANCELED = "canceled",
  ENDED = "ended",
  CONNECTED = "connected",
}
export enum CALL_DETAILS {
  RESPONDING = "responding",
}
