/* eslint-disable no-unused-vars */

/**
 * THis function takes an stream and a media and adds it to the docs
 */
export default function appendStreamToPage(media: HTMLMediaElement, stream: STREAM, errCb?: (e: ERROR) => {}): void;
